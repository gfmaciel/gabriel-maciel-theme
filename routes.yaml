routes:
  /:
    template: home
    data: page.home
  /artigos/:
    template: artigos-redirect

collections:
  /posts/:
    permalink: /posts/{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
